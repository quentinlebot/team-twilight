.navbar.navbar-inverse.navbar-fixed-top(role='navigation')
    .container
        .navbar-header
            button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
            span.sr-only Toggle navigation
            span.icon-bar
            span.icon-bar
            span.icon-bar
                a.navbar-brand(href='#') Team Twilight
        .collapse.navbar-collapse
            ul.nav.navbar-nav
                li
                    a(href='/') Scores
                li
                    a(href='/map') Générateur de carte
                li
                    a(href='/racePick') Tirage des Races
                if user
                    li
                        a(href='/gameresult') Résultas de partie
                    li
                        a(href='/player') Joueurs
            if user
                ul.nav.navbar-nav.navbar-right
                    li
                        a(href='#') #{user.login}
                    li
                        a(href='/signout') Deconnexion
            else
                form.navbar-form.navbar-right(id='submit' method='POST', action='/signin')
                    input#login.form-group.form-control(type="text" name='login' placeholder='login')
                    input#password.form-group.form-control(type="password" name='password' placeholder='password')
                    input.form-group.btn.btn-success(type='submit' value='Connexion')
script.
    $("#submit").submit(function( event ) {
        if(!login()) event.preventDefault();
    });

    function login() {
        var isValid = true;

        if($('#login input').val() == ''){
            isValid = false;
            $('#login').addClass('alert.alert-danger');
        }else $('#login').removeClass('alert.alert-danger');

        if($('#password input').val() == ''){
            isValid = false;
            $('#password').addClass('alert.alert-danger');
        }else $('#password').removeClass('alert.alert-danger');

        return isValid;
    };